require "spec_helper"

RSpec.describe Pem2xml do
  it "has a version number" do
    expect(Pem2xml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
